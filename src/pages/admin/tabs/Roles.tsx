import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function Roles() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Roles & Permissions</CardTitle>
      </CardHeader>
      <CardContent>
        Manage roles and access levels for all users.
      </CardContent>
    </Card>
  );
}
