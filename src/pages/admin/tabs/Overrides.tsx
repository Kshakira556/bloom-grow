import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function Overrides() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Override Decisions</CardTitle>
      </CardHeader>
      <CardContent>
        Review and manage any override decisions in the system.
      </CardContent>
    </Card>
  );
}
